CREATE TABLE IF NOT EXISTS login_course.users(
    user_id INT(5) NOT NULL PRIMARY KEY AUTO_INCREMENT,
    email VARCHAR(250) NOT NULL UNIQUE,
    password VARCHAR(200) NOT NULL,
    reg_time TIMESTAMP
);